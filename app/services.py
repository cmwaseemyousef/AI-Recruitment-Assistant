def analyze_resume(resume_text):
    # Dummy analysis logic (Replace with AI model later)
    word_count = len(resume_text.split())
    return {"resume_analysis": f"The resume contains {word_count} words."}
